apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/part-of: pns-urp-bootstrap
      bootstrap.pns-urp.io/owner: config-sync

resources:
  - namespace.yaml
  - app-of-apps.yaml

generators:
  - argocd-install.yaml
